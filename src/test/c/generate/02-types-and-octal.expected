#include <stdio.h>

enum Color {
    RED,
    GREEN = 2,
    BLUE
};

struct Point {
    int x;
    int y;
};

int origin(void);

int mask = 017;

int origin(void) {
    struct Point p;
    ((p).x = 0);
    return ((p).x + mask);
}
