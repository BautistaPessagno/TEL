static const unsigned long int limit = 017;
int values[3] = {1, 2, 3};
int *pointer = NULL;

int compute(int data[], int size) {
    int total = 0;
    int i = 0;
    total += data[i];
    return total + limit;
}
